spring:
  datasource:
    url:  jdbc:mysql://localhost:3306/demodb?verifyServerCertificate=false&useSSL=false&requireSSL=false
    username: root
    password: Root@123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 9002

